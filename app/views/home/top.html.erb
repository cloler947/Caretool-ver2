<h1>Care tool</h1>

<div class="main-visual">

</div>

<h2>いいねランキング</h2>
<div class="ranking">
    <% @posts.each.with_index(1) do |post, i| %>
      <div class="rank-item">
        <div class="ranking-num"><%= "#{i}位"%></div>
        <div class="rank-image"><%= attachment_image_tag(post, :image, :fill, 250, 250, fallback: "写真のフリー素材11.jpeg", size: "250x250") %></div>
        <div class="rank-title"><%= link_to post.title , post_path(post.id) %></div>
        <div class="rank-user"><%= post.user.name %></div>
      </div>
    <% end %>
</div>