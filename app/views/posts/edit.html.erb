<h1>記事編集</h1>

<%= render "layouts/errors", obj: @post %>

<%= form_for(@post) do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title %>

	<%= f.label :body %>
	<%= f.text_area :body %>

	<%= f.attachment_field :image %>

	<%= f.submit "編集を保存する" %>
<% end %>
