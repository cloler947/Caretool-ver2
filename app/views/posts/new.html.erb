<h1>記事新規作成</h1>

<%= form_for(@post) do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title %>

	<%= f.label :body %>
	<%= f.text_area :body %>

	<%= f.attachment_field :image %>


	<%= f.submit "新規作成" %>
<% end %>
